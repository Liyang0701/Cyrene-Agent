# Share user documents but isolate character knowledge

User-imported PDFs, Markdown, Word files, and similar material belong to a Global Document Library available to every character, while package worldbooks and conversation-derived memory remain scoped to their Character ID. Retrieved global passages support the current request but cannot automatically become character lore or long-term memory; this avoids duplicate uploads without allowing one character’s identity or relationship context to leak into another.
