# 昔涟 (Cyrene) Agent 架构文档

> 最后更新：2026-06-16
> 项目路径：`C:\Users\13575\Documents\live2D-Cyrene`
> 项目概括：是基于《崩坏：星穹铁道》的角色昔涟（Cyrene）为人设开发的兼顾情感陪伴的Agent
> 额外内容：live2D桌宠+桌面端
---

## 一、整体架构概览

```
用户输入
    │
    ▼
┌─────────────────────────────────────────────────┐
│  Orchestrator（调度层）                          │
│  ┌────────────────┐  ┌──────────────────────┐   │
│  │ Rule Router    │  │ LLM Router (规划中)  │   │
│  │ (当前, 纯本地) │→→│ (OpenClaw 风格)     │   │
│  │ 关键词匹配     │  │ 语义理解 + 上下文    │   │
│  └───────┬────────┘  └──────────┬───────────┘   │
│          │                      │               │
│          ▼                      ▼               │
│  ┌────────────────────────────────────────┐     │
│  │        OrchestratorPlan               │     │
│  │  useUserMemory?  useWorldbook?        │     │
│  │  useImportedDocs? useWebSearch?       │     │
│  │  shouldWriteMemory? shouldReflect?    │     │
│  └───────────────────┬────────────────────┘     │
└──────────────────────┼──────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────┐
│  Context Builder                                 │
│  根据 Plan 按需调用：                            │
│  ├── getPermanentWorldbookEntries() → 常驻背景  │
│  ├── searchWorldbook()            → 关键词触发  │
│  ├── searchMemory(imported_doc)   → 导入文档    │
│  ├── searchMemory(user_memory)    → 用户记忆    │
│  └── Reranker (可选)              → 重排序      │
└───────────────────┬─────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────┐
│  Prompt Builder                                  │
│  拼装顺序：                                      │
│  system.md → identity.md → soul.md              │
│  → canon_quotes.md → style.md                   │
│  → 【常驻背景】 → 【相关背景】                    │
│  → 【相关文件片段】 → 【关于用户的记忆】          │
│  → 聊天历史 → 用户输入                           │
└───────────────────┬─────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────┐
│  LLM 主聊天                                      │
│  (当前: MiniMax-M3, 流式输出)                    │
└───────────────────┬─────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────┐
│  后处理                                          │
│  ├── inferRuntimeState()  → 状态/表情/心情      │
│  ├── inferStickerId()     → 表情包              │
│  ├── observeRuntimeState() → LLM 心情观察器     │
│  └── 广播到各窗口                                │
└─────────────────────────────────────────────────┘
```

---

## 二、Prompt 架构

### 文件结构

```
prompts/
├── system.md              # 全局系统规则（输出格式、安全规则、工具约束）
├── identity.md            # 身份定位（用户可自定义上传）
├── soul.md                # 人格核心（性格、价值观、行为原则）
├── canon_quotes.md        # 原作台词
├── styles/                # 用户可切换的聊天风格
│   ├── 01_default.md      # 默认
│   ├── 02_lively.md       # 活泼
│   ├── 03_healing.md      # 治愈
│   ├── 04_focused.md      # 专注
│   └── 05_sweet.md        # 甜腻
└── worldbook/             # RAG 按需召回（4个文件，30+条目）
    ├── Cyrene.md           # 昔涟自身（7条）
    ├── characters.md       # 相关人物
    ├── story.md            # 故事事件
    └── world.md            # 世界观设定
```

### 拼装顺序

```
system.md
    ↓
identity.md
    ↓
soul.md
    ↓
canon_quotes.md
    ↓
用户当前选择的 style.md
    ↓
【常驻背景】  ← Worldbook 常驻条目（标记了"常驻: 是"的条目，每轮都注入）
    ↓
记忆上下文（由 Orchestrator 决定是否加入）
    ├── 【相关背景】   ← Worldbook 关键词触发检索（纯 BM25，无向量）
    ├── 【相关文件片段】 ← Imported Docs 检索结果（经 Reranker 重排）
    └── 【关于用户的记忆】 ← User Memory 检索结果（经 Reranker 重排）
```

---

## 三、调度层（Orchestrator）

### 文件

```
src/main/orchestrator/
├── types.ts              # OrchestratorPlan, RuleContext, Rule 接口
├── rule-router.ts        # 6 条注册式规则，优先级排序
├── context-builder.ts    # 根据 Plan 执行检索并拼装上下文
└── index.ts              # 统一入口 buildOrchestratedMemoryContext()
```

### 当前：Rule Router（纯本地关键词匹配）

| 规则 | 优先级 | 触发条件 | 作用 |
|------|--------|---------|------|
| explicit_document_question | 90 | 关键词：文件/文档/小说/总结/分析/内容 | 启用导入文档检索 |
| entity_plot_question | 85 | 专有名词+因果问题 | 启用导入文档检索 |
| user_memory_question | 80 | 关键词：你还记得/我之前说/以前 | 启用用户记忆检索 |
| worldbook_trigger | 75 | 关键词：昔涟/星灵/背景/经历 | 启用世界书检索 |
| web_search_trigger | 70 | 预留，v1 不启用 | 启用网络搜索 |
| daily_chat | 10 | 永远匹配（fallback） | 默认不检索 |

**已知问题：**
- daily_chat 永远 true，会覆盖高优先级规则的设置
- 关键词匹配脆弱，无法理解上下文
- 用户连续聊同一话题时，第二轮可能漏检

### 规划中：LLM Router（OpenClaw 风格）

> 由 LLM 语义理解决定是否需要检索，返回 JSON 格式的 Plan。

---

## 四、RAG 系统

### 文件结构

```
src/main/rag/
├── index.ts           # 统一入口：initRAG, searchMemory, searchWorldbook, importDocument
├── embedding.ts       # Embedding Provider（本地 MiniLM / BGE-M3 / OpenAI 兼容）
├── retriever.ts       # HybridRetriever（Vector + BM25 加权融合）
├── vectorstore.ts     # JsonVectorStore（JSON 文件存储 + 余弦相似度）
├── worldbook.ts       # WorldbookManager（纯关键词触发，无向量）
├── chunk.ts           # 文档切片
└── reranker.ts        # Reranker（轻量 ms-marco-MiniLM / 标准 bge-reranker-base）
```

### 检索流程

```
用户输入
    │
    ▼
Orchestrator 判断是否需要检索
    │
    ├── Worldbook 检索（关键词触发，纯 BM25）
    │   ├── 匹配触发词 → 返回匹配条目（按优先级排序，Top-3）
    │   └── 无匹配 → 不检索
    │
    ├── Imported Docs 检索（Hybrid: Vector 0.7 + BM25 0.3）
    │   ├── Top-10 候选
    │   └── Reranker（可选）→ Top-3
    │
    └── User Memory 检索（Hybrid: Vector 0.7 + BM25 0.3）
        ├── Top-10 候选
        └── Reranker（可选）→ Top-3
```

### Embedding 模型

| 模型 | 大小 | 维度 | 状态 |
|------|------|------|------|
| all-MiniLM-L6-v2 | ~23MB | 384 | ✅ 当前使用 |
| BGE-M3 | ~570MB | 1024 | ✅ 已安装，可选 |

### Reranker 模型

| 选项 | 模型 | 架构 | 大小 | 延迟 | 状态 |
|------|------|------|------|------|------|
| 轻量 | ms-marco-MiniLM-L6-v2 | Cross-Encoder | ~23MB | ~50ms/对 | ✅ 默认 |
| 标准 | bge-reranker-base | Cross-Encoder | ~279MB | ~200ms/对 | ✅ 可选 |
| 关闭 | — | — | — | 0ms | ✅ 可选 |

---

## 五、Worldbook 系统

### 文件格式

```markdown
# 文件名.md — 描述
> 描述文字
---

## 条目名
- 触发词: 词1, 词2, 词3
- 常驻: 是/否
- 优先级: 200

内容段落...
---

## 下一个条目
- 触发词: ...
```

### 条目类型

| 类型 | 字段 | 行为 |
|------|------|------|
| **常驻** | `常驻: 是` | 每轮聊天都注入 Prompt，不依赖关键词 |
| **关键词触发** | `常驻: 否`（默认） | 用户提到触发词时才检索 |

### 当前文件

| 文件 | 条目数 | 内容 |
|------|--------|------|
| Cyrene.md | 7 条 | 昔涟自身设定（权杖之心、三形态、无漏净子、善见天等） |
| characters.md | 多条 | 相关人物（白厄、来古士、荒笛、风标等） |
| story.md | 多条 | 故事事件（哀丽秘榭、逐火之旅、铁幕决战等） |
| world.md | 多条 | 世界观设定（权杖、火种、黄金裔等） |

---

## 六、记忆系统（三层画像）

### L0：长期画像（Core）
- 长期稳定信息，很少变化
- 存储在 UserProfile 接口中
- 支持用户编辑（设置面板 → 用户信息）

### L1：阶段画像（Stage）
- 当前阶段目标和状态
- 由 Reflection 或 Memory Judge 更新
- 规划中，尚未实现

### L2：事件记忆（Episode）
- 碎片化事件和导入文档
- 存储在 userData/rag-data/ 的 JSON 文件中
- 使用 Hybrid (Vector + BM25) 检索

---

## 七、运行时状态系统

### 状态字段

```typescript
interface RuntimeState {
  status: "陪伴中" | "思考中" | "工作中" | "聆听中" | "提醒中" | "离线";
  feeling: "平静" | "开心" | "温柔" | "激动" | "撒娇" | "担心" | "难过" | "感动" | "害羞";
  expression: number;  // 对应 Live2D 表情索引
  updatedAt: number;
}
```

### 更新方式（用户可设置）

| 模式 | 方式 | Token 消耗 |
|------|------|-----------|
| 关闭 | 不更新，保持默认 | 无 |
| 本地关键词 | 关键词匹配推断 | 无 |
| LLM 分析 | 每轮额外调一次 LLM | 额外消耗 |

---

## 八、表情包系统

| ID | 触发场景 |
|----|---------|
| playful | 撒娇/调皮 |
| love-happy | 开心/爱意 |
| confident | 自信/得意 |
| shy | 害羞 |
| surprised | 惊讶 |
| sad | 难过 |
| angry | 生气 |
| tired | 疲惫 |
| neutral | 平静/默认 |

---

## 九、设置面板结构

### 导航

| 面板 | 状态 |
|------|------|
| ⚙️ 设置 | ✅ 已完成 |
| 🔑 API 设置 | ✅ 已完成 |
| 🌸 昔涟设置 | ✅ 已完成 |
| 🧠 记忆 | ✅ 已完成 |
| 👤 用户信息 | ✅ 已完成 |
| 📜 身份 | ✅ 已完成 |
| ✨ 技能 | ✅ 已完成 |
| 🔌 插件 | ✅ 已完成 |
| 🎙️ TTS 设置 | ✅ 已完成 |
| 📊 Token 用量 | ✅ 已完成 |
| 📜 免责声明 | ✅ 已完成 |

### 昔涟设置子模块

| 模块 | 状态 |
|------|------|
| 状态栏实时更新 | ✅ 已完成 |
| 表情包 | ✅ 已完成 |
| RAG / 文档导入 | ✅ 已完成 |
| 推理强度 | ✅ 已完成 |
| 聊天风格 | ✅ 已完成 |
| 职位 | ⏳ 待开发 |
| 定时任务 | ⏳ 待开发 |

---

## 十、数据流（完整链路）

```
用户发送消息
    │
    ▼
Chat Renderer (src/renderer/chat/main.ts)
    │  IPC: chat:send-message
    ▼
Main Process (src/main/index.ts)
    │
    ├── loadModelSettings()          → 读取 API Key、模型配置
    ├── normalizeChatMessages()      → 标准化消息格式
    ├── buildOrchestratedMemoryContext()
    │   ├── route()                  → Rule Router 生成 Plan
    │   └── buildOrchestratedContext() → 按 Plan 执行检索
    │       ├── getPermanentWorldbookEntries() → 常驻背景（始终注入）
    │       ├── searchWorldbook()    → 世界书关键词触发
    │       ├── searchMemory(imported_doc) → 导入文档
    │       │   └── rerankResults()  → Reranker 重排（可选）
    │       └── searchMemory(user_memory) → 用户记忆
    │           └── rerankResults()  → Reranker 重排（可选）
    │
    ├── buildSystemPrompt()          → 拼装 Prompt
    ├── callChatCompletionsStream()  → 调 LLM API（流式）
    │   ├── 逐 chunk 广播到聊天窗口
    │   └── 返回完整回复
    │
    ├── inferRuntimeState()          → 推断状态/表情
    ├── inferStickerId()             → 推断表情包
    ├── observeRuntimeState()        → LLM 心情观察（可选）
    │
    └── 广播到各窗口
        ├── chat:stream-done         → 聊天窗口
        └── runtime-state-changed    → 侧边栏/宠物窗口
```

---

## 十一、技术栈

| 层 | 技术 |
|----|------|
| 桌面框架 | Electron |
| 语言 | TypeScript |
| 构建 | Vite + tsc |
| 前端 | 原生 HTML/CSS/TS（无框架） |
| 聊天 | 流式 SSE（Server-Sent Events） |
| Embedding | @xenova/transformers (ONNX Runtime) |
| 向量存储 | 本地 JSON 文件 + 余弦相似度 |
| BM25 | 自实现（无外部依赖） |
| Reranker | @xenova/transformers (Cross-Encoder) |
| 模型格式 | ONNX (model_quantized.onnx) |
| 通信 | Electron IPC (contextBridge) |
| 状态管理 | 本地 JSON 文件 + localStorage |
| Live2D | Cubism SDK |

---

## 十二、已知问题 / 待办

### 调度层
- [ ] daily_chat 规则覆盖高优先级规则的设置
- [ ] 关键词匹配无法理解上下文
- [ ] 计划引入 LLM Router（OpenClaw 风格）

### RAG
- [x] Worldbook 已重构完成（4 文件，30+ 条目，纯关键词触发）
- [ ] 导入文档在无关键词时被无条件检索（已部分修复）
- [x] Reranker 切换（设置面板可切换轻量/标准/关闭）

### 记忆
- [ ] L1 阶段画像（Reflection）未实现
- [ ] Memory Judge（判断是否写入记忆）未实现
- [ ] 记忆面板的编辑/删除/固定功能未实现

### 插件
- [ ] 所有插件功能待开发（当前仅 UI）

### 其他
- [ ] 心情观察器超时（3秒太短）
- [ ] 拖拽上传文件功能未实现
- [ ] 模型下载经常超时（网络问题）
